name: PR checks

on:
  pull_request:

permissions:
  contents: read

jobs:
  jvm-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          java-version: '25'
          distribution: temurin
      - uses: gradle/actions/setup-gradle@v6
      - name: Generate grammar
        run: ./gradlew --no-daemon :coordinates:generateKotlinGrammarSource
      - name: Test and validate every module on JVM
        run: >-
          ./gradlew --no-daemon
          :calculation:jvmTest
          :geojson:jvmTest
          :ogc:jvmTest
          :interop:jvmTest
          :coordinates:jvmTest
          :fwi:jvmTest
